# راهنمای مدرس — ایده‌های بهبود کلاس‌ها

مخاطبان شما **معلم کامپیوتر** هستند؛ یعنی تئوری را می‌دانند ولی شاید پایتون و کاربرد عملی را کمتر. این تفاوت را در طراحی هر کلاس لحاظ کنید.

---

## تفاوت مخاطبان و پیشنهاد برای هر کلاس

### ۱. پایتون مقدماتی (۴ جلسه)
**مخاطب احتمالی:** کسانی که پایتون بلد نیستند یا خیلی کم کار کرده‌اند.

| ایده | چرا |
|------|-----|
| مثال‌های مرتبط با مدرسه | لیست نمرات، حضور و غیاب، محاسبه معدل |
| تمرین ویس از جلسه ۲ | عادت توضیح دادن از اول شکل بگیرد |
| «ترجمه» از زبان دیگر | اگر C/Java بلدند: «در پایتون لیست مثل آرایه است ولی...» |
| تمرین خانه کوتاه | ۱۵–۲۰ دقیقه، نه پروژه سنگین |
| جلسه ۴ = پروژه کاربردی | مثلاً برنامه ساده مدیریت نمره کلاس |

---

### ۲. شی‌گرایی ۸ جلسه
**مخاطب احتمالی:** وقت بیشتر برای عمق و تمرین.

| ایده | چرا |
|------|-----|
| مقایسه با Java/C++ | آن‌ها OOP دانشگاه را با `public class` یاد گرفته‌اند |
| جدول تفاوت پایتون vs Java | `self` vs `this`، نبود `private` واقعی، `@property` |
| پروژه تدریجی ۸ جلسه | مثلاً سیستم کتابخانه: جلسه ۱ `Book`، جلسه ۳ `Member`، جلسه ۵ وراثت |
| Pair programming | دو نفر یک لپ‌تاپ — برای معلم‌ها خوب جواب می‌دهد |
| تمرین «باگ عمدی» | کدی بدهید که class/instance attribute قاطی شده؛ پیدا و درست کنند |

---

### ۳. شی‌گرایی فشرده (۴ جلسه) ← کلاس فعلی شما
**مخاطب:** OOP را می‌دانند؛ فقط پایتون و سبک آن را یاد می‌گیرند.

| ایده | چرا |
|------|-----|
| **تمرین ویس** (مثل جلسه ۱) | سریع‌ترین راه فهمیدن «فهمیده یا حفظ کرده» |
| سرعت بالاتر | کمتر تعریف OOP، بیشتر «در پایتون چطور» |
| تمرین‌های شغلی | کلاس `Teacher`، `DaneshAmooz`، `Kelas` — به دنیای خودشان نزدیک است |
| چالش جلسه ۳–۴ | یک mini-project: مدیریت چند کلاس با وراثت |
| اشتباهات رایج را جلو بگیرید | `self` فراموش، attribute مشترک به‌اشتباه در `__init__`، mutable default |

**برای تمرین ویس:** اگر در ویس گفت «self یعنی خود کلاس» → هنوز نفهمیده.  
اگر گفت «self یعنی همین آبجکتی که الان داریم باهاش کار می‌کنیم، مثلاً m1» → فهمیده.

---

### ۴. جنگو (۱۰ جلسه)
**مخاطب:** احتمالاً بعد از پایتون/OOP؛ می‌خواهند وب بسازند.

| ایده | چرا |
|------|-----|
| پروژه واقعی از جلسه ۱ | «سایت معرفی مدرسه» بهتر از todo ساده |
| ارتباط با OOP | «Model یعنی کلاس، instance یعنی رکورد دیتابیس» |
| deploy روی یک سرویس رایگان | حس accomplishment — حتی اگر فقط یک صفحه |
| تمرین بین جلسه = یک feature | «فقط صفحه درباره ما را اضافه کن» |
| جلسه ۱۰ = دمو برای همکلاسی‌ها | انگیزه خوب برای معلم‌ها |

---

## ایده‌های عمومی برای همه کلاس‌ها

### ۱. تمرین ویس (پیشنهاد قوی)
- بعد از هر جلسه مهم، ۳–۵ سوال مفهومی + یک کد کوچک
- معیار: **توضیح با زبان خودش**، نه خواندن کد
- زمان ویس را محدود کنید (۳–۵ دقیقه) تا حوصله نکنند ولی مجبور به خلاصه‌گویی شوند

### ۲. کانال/گروه مشترک
- یک thread برای هر جلسه
- پاسخ تمرین‌ها را آنجا بگذارند
- شما فقط ویس‌ها را گوش دهید؛ کد را می‌توانید با نگاه سریع چک کنید

### ۳. «کارت اشتباه رایج»
هر جلسه یک اشتباه رایج را صریح بگویید:
- جلسه OOP: `self` ننوشتن
- جلسه Django: فراموش کردن `migrate`
- جلسه پایتون: `=` به‌جای `==` در if

### ۴. تفاوت سطح در یک کلاس
اگر در یک کلاس هم سطح‌ها متفاوت است:
- **پایه:** تمرین اصلی را کامل کنند
- **پیشرفته:** یک extension اختیاری (مثلاً متد `hazf_kelas` یا `__str__`)

### ۵. بازخورد سریع
برای ویس‌ها یک قالب پاسخ کوتاه:
```
✓ سوال ۱ و ۲ — درست
△ سوال ۳ — self را درست گفت ولی زمان اجرای init را قاطی کرد
✗ سوال ۴ — local variable را با instance attribute اشتباه گرفت
→ جلسه بعد ۵ دقیقه مرور attributeها
```

---

## چک‌لیست قبل از هر جلسه

- [ ] تمرین جلسه قبل بررسی شد (ویس یا کد)
- [ ] ۲–۳ دقیقه مرور اشتباهات رایج جلسه قبل
- [ ] مثال جدید مرتبط با کار معلمی
- [ ] تمرین خانه برای جلسه بعد آماده است

---

## جلسه بعد شی‌گرایی فشرده (پیشنهاد)

**موضوع:** وراثت + polymorphism  
**تمرین پیشنهادی:**  
- کلاس پایه `Person` → `Teacher` و `DaneshAmooz`  
- override متد `moarefi()`  
- ویس: «چرا از وراثت استفاده کردیم به‌جای دو کلاس جدا؟»
